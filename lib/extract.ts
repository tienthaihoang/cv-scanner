/* eslint-disable @typescript-eslint/no-unused-vars */
// Text extraction disabled — raw file will be uploaded directly to OpenAI

export async function extractTextFromFile(file: File) {
  return ""; // No parsing
}

export async function ocrFallback(buffer: Buffer) {
  return ""; // Disabled
}
